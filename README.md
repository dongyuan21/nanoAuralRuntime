# nanoAuralRuntime

An audio-native, model-agnostic runtime, workflow SDK, and durable serving stack
for audio foundation models.

ControlFoley is the first model adapter and vertical workflow, not the project boundary.

Status: architecture bootstrap / pre-alpha.
